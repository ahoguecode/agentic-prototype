import { Provider, defaultTheme } from '@adobe/react-spectrum'
import './utils/IMS'

function App() {
  return (
    <Provider theme={defaultTheme}>
      <div>
      </div>
    </Provider>
  )
}

export default App
